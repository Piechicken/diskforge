# DiskForge 真实文件系统验收

**DiskForge 的常规测试不包含第三方二进制磁盘映像。** 这是为了保持源代码仓库轻量、可审查，并避免重新分发不属于项目的样本。与真实文件系统映像有关的回归是可选验收：只有在操作者自行取得相应样本、确认其许可并在本机安装 Sleuth Kit `fls` 和 `icat` 后才会执行。

> 真实样本验收确认的是 DiskForge 的**只读列举和数据 fork 提取**路径。另有独立的合成映像回归覆盖可选 NTFS/EXT/经典 HFS 受控注入，以及经典 HFS 的受限新建：它们只创建新输出映像并验证文件内容或签名，绝不修改源映像或物理设备。经典 HFS 注入只复制原始数据 fork；该范围不授权或实现 HFS+ 写入、文件系统修复、自动挂载、资源 fork 完整保留，也不保证每一种损坏、加密、压缩或专有容器变体均可读取。

## 可选夹具

| 文件名 | 用途 | 来源与许可 | DiskForge 调用方式 |
|---|---|---|---|
| `ntfs-lastaccess.raw` | NTFS 分区级列举与 `/test/1.txt` 提取 | `msuhanov/ntfs-samples`，仓库标示 CC0-1.0。[1] | `FileSystemType.NTFS`，显式偏移 `128 * 512` 字节。 |
| `fs.ext4` | EXT4 分区级列举与 `/audio1/debian.mp3` 提取 | `eribertomota/forensics-samples`，文件系统映像标示为 MIT 许可。[2] | `FileSystemType.EXT`，显式偏移 `2048 * 512` 字节。 |
| `nps-hfsj-image.gen1.dmg` | journaled HFS+ 根目录列举与 `/file1.txt` 数据 fork 提取 | Digital Corpora NPS 公共测试映像，目录为 10 MB。[3] [4] | `FileSystemType.HFS_PLUS`，偏移 0。 |

将文件放入一个**不受版本控制**的目录。不要将样本、从样本提取的个人数据或本地绝对路径提交到 DiskForge 仓库。

## 运行方式

在 Linux 或具备同等 Sleuth Kit 后端的环境中，设置样本目录后运行：

```bash
cd diskforge
export DISKFORGE_REAL_FS_FIXTURES=/absolute/path/to/fixtures
QT_QPA_PLATFORM=offscreen pytest -W error tests/test_real_readonly_samples.py
```

这会运行 `tests/test_real_readonly_samples.py`。当环境变量、所需样本或 Sleuth Kit 工具不可用时，测试会以明确的 `skipped` 状态结束；它不会下载样本、挂载映像、修改映像或把外部工具静默打包到程序中。

## v0.10 自动化老式映像与 ISO 验收

v0.10 的常规严格回归不需要稀缺物理软盘。它会创建并重新打开 15 个可验证的 PC 兼容 FAT12 profile：5.25 英寸 160/180/320/360/640/720 KiB 与 1.2 MiB，3.5 英寸 320/360/640/720 KiB、1.44 MiB、DMF 1.68 MiB、82 磁道 1,722 KiB 与 2.88 MiB。每个 profile 均验证输出 `.ima`/`.img` 格式、文件长度、FAT12 识别及 BPB 的扇区大小、每磁道扇区数和磁头字段。回归还验证 IMA 的注入、改名、DOS 属性、提取、SHA-256 以及 IMG/IMA 无损转换。

ISO 回归会验证标准、Rock Ridge、UDF 和组合 profile 的重建编辑；对于 El Torito，仅接受并复核可唯一映射的单初始启动条目。多节/多启动目录、混合系统区、启动映像或自动生成目录的修改、以及无法无歧义重建的映像均必须失败。此限制是可验证安全边界，而非对全部启动 ISO 变体的虚假兼容声明。

128/256 字节扇区、GCR、变速/变扇区、硬分区、非 FAT、复制保护与 flux/bitcell 格式不进入原生 FAT 创建断言；它们仍可按原始字节进行保存、校验、比较和受控转换。测试不会根据文件大小臆测这些格式的物理来源。

## v0.10 可选 NTFS/EXT/经典 HFS 受控注入与新建验收

NTFS、EXT 和经典 HFS 写入均不作为原生或跨平台保证。仅当本机明确提供所需外部后端时，常规回归才会对临时创建的独立映像执行该项验收：NTFS 使用 `mkntfs`、`ntfscp`、`ntfsls` 和 `ntfscat`；EXT 使用 `mke2fs`、`debugfs` 和 `e2fsck`；经典 HFS 使用 `hformat`、`hmount`、`hcopy` 和 `hls`。缺少任一可选工具时，对应集成用例以明确 `skipped` 状态结束；不下载、不打包任何外部二进制。[6] [7] [8] [9]

| 文件系统 | 合成验收 | 必须保持的边界 |
|---|---|---|
| NTFS | 创建 64 MiB 独立卷；`ntfscp -n` 预演；将新常规文件写入独立输出；用 `ntfscat` 读回 SHA-256，并重新计算源 SHA-256。 | 拒绝设备、分区偏移、已有目标、覆盖、目录、ADS、ACL、元数据、改名、删除及原位写入。 |
| EXT4 | 创建 64 MiB 独立卷；生成受限命令文件，由 `debugfs -w -f -z` 写入独立输出；`debugfs dump` 读回 SHA-256；`e2fsck -fn` 必须返回 0。 | 拒绝设备、分区偏移、已有目标、目录、链接、元数据、改名、删除及原位写入。 |
| 经典 HFS 注入 | 创建 800 KiB 独立卷；每次操作使用独立临时 `HOME`；在新副本挂载后，必须从 `hls -1 -N` 的 stderr 检出 `no such file or directory` 再执行 `hcopy -r`；逐项 `hcopy -r` 读回 SHA-256，并重算源 SHA-256。 | 拒绝设备、分区偏移、已有目标、目录、链接、冒号/通配符名称、MFS、HFS+、元数据、资源 fork、改名、删除及原位写入。 |
| 经典 HFS 新建 | 对新的 800 KiB、512 字节对齐临时常规文件执行 `hformat -l`；使用独立临时 `HOME`；验证 HFS 签名、大小和输出 SHA-256，再原子提升。核心、CLI JSON、图形新建对话框与 v4 `hfs_create` recipe 均有回归。 | 拒绝小于 800 KiB 或非 512 对齐大小、既有目标、设备路径、分区号、`-f`、分区映射、MFS、HFS+、物理介质和不安全卷标。 |

这些测试还覆盖 GUI 的新输出选择与自动打开、CLI 的 JSON 审计结果，以及批处理 v4 的 `ntfs_inject`/`ext_inject`/`hfs_inject`/`hfs_create` 预览和执行。HFS+ 仍不启用受控注入或新建动作。详细合同见 [FILESYSTEM_INJECTION.md](FILESYSTEM_INJECTION.md)。

## v0.10 显式只读分区浏览与目录报告验收

DiskForge 的 MBR/GPT 解析器先验证分区表，再只接受操作者指定的稳定一位索引；它不会扫描或推断“第一个可兼容分区”。FAT 分区保持已有的可写会话，而 NTFS、EXT、经典 HFS 与 HFS+ 分区仅由 Sleuth Kit `fls`/`icat` 在该分区的精确扇区偏移下打开。请求非 FAT 写入会在启动外部后端之前被拒绝；不挂载、不修改分区表、不写入源映像或物理设备。[8] [9]

| 验收层 | 自动化证据 | 必须保持的边界 |
|---|---|---|
| 核心路由 | 合成 MBR Linux 分区验证 EXT 类型与精确字节偏移；写入请求在后端调用前失败；未知类型拒绝。 | 仅 FAT 可请求写入；NTFS/EXT/HFS/HFS+ 永远只读，且必须显式选择索引。 |
| CLI 与 SDK | `list`、`extract`、`export-listing` 与 `DiskForgeClient.filesystem(..., partition_index=N)` 均将索引交给同一核心路由，并始终关闭会话。 | 不回退为 FAT 或 offset-0；无隐式选择、无自动挂载、无写入升级。 |
| GUI 与报告 | 分区对话框可选已验证非 FAT 分区；导出和打印通过统一完整遍历产生新的文本/HTML 本地报告。 | 报告只写入所选报告文件；注入、删除、改名及受控写入动作仍禁用。 |

通用目录报告回归还验证跨文件系统稳定排序、HTML 路径转义和取消时不创建输出文件。该机制仅序列化 `ImageFilesystem.walk_entries()` 的只读结果，不执行文件、脚本或映像内内容。

## v0.10 ZIP 单映像容器只读验收

普通 `.zip` 只可作为受控的**单映像只读容器**。核心只接受恰好一个根级常规载荷：名称不含路径、NUL、驱动器成分或目录遍历；条目未加密；方法仅为 Stored/Deflated；大小为正且不超过 2 GiB；后缀为 `.img`、`.ima`、`.bin`、`.dd`、`.dmf`、`.iso` 或 `.hfs`；物化后还必须重新识别为可浏览文件系统。载荷采用流式复制写入私有临时目录，关闭、异常或取消时清除；源 ZIP 的 SHA-256 保持不变。

| 验收层 | 自动化证据 | 必须保持的边界 |
|---|---|---|
| 核心 | 合成 FAT ZIP 物化后重开、列举、逐字节 SHA-256 对照与清理；拒绝多条目、目录条目、嵌套路径、非认可后缀、BZIP2、加密标志、大小上限、无法识别载荷；取消时验证临时目录已删除。 | ZIP 不是通用解压器、递归容器、虚拟磁盘转换链或可写映像；不写回源容器。 |
| CLI 与 SDK | CLI JSON `list`、`extract`、`export-listing` 和 SDK `filesystem()`/`extract()` 均经过统一临时只读会话。 | `inject`、`move-fat`、`writable=True` 和转换 ZIP 源均在任何映像写入前失败。 |
| 批处理与桌面 | v4 `extract`/`export_listing` 可读取容器；写入 recipe 产生可审计失败。桌面以临时只读 FAT 会话显示内容，提取、预览和报告可用。 | 桌面禁用注入、删除、移动、启动、部署、转换、调整大小和零尾裁剪；关闭映像清除临时目录。 |

## v0.10 IMD 只读检查与严格 RAW 导出验收

IMD 被作为扇区记录容器解析，不会被自动降级为可写 RAW 映像。核心仅在轨道集合能够证明完整矩形 CHS、固定扇区数量和大小、连续 `1..N` 扇区号、无可选 cylinder/head map 且所有扇区数据类型为正常或正常压缩填充时，才将扇区按 cylinder、head、sector 顺序流式写入新的 RAW 文件。所有其他有效 IMD 仍可检查，但不能导出。

| 验收层 | 自动化证据 | 必须保持的边界 |
|---|---|---|
| 核心解析与导出 | 合成 IMD 覆盖普通与压缩填充扇区、描述文本、严格 CHS 证明、RAW 字节顺序、源 SHA-256 不变和已有输出拒绝。 | 无 IMD 源写入、无覆盖、无设备输出；仅创建新本地 RAW 文件。 |
| 结构拒绝 | 覆盖截断头/轨道、未知映射、缺失/删除/坏扇区、重复轨道、非矩形几何、尾随字节和不可证明布局。 | 不猜测、补零、重排或修复扇区；不能导出不完整或非正常数据。 |
| CLI、SDK 与桌面 | CLI JSON `imd-info`/`convert-imd`、SDK `inspect_imd()`/`export_imd_to_raw()`、离屏 GUI 工具入口、文件选择器直达只读检查器和七语种文本均有严格回归。 | 不接入无人值守批处理、磁通/位流处理、IMD 写入或物理媒体读写。 |

## v0.10 TD0 只读检查与严格 RAW 导出验收

TD0 被作为历史软盘扇区容器解析，不会因后缀而被当作可写 RAW 或 FAT 映像。初版仅接受普通未高级压缩的 `TD` 记录，验证 A097 头、可选注释、磁道和扇区 CRC，并检查原始、重复双字节和 RLE 扇区数据是否精确重建为声明长度。检查与导出分离：只有无标志、精确 EOF、完整零起点矩形 CHS、固定几何、逻辑/物理 CHS 一致且 ID 为连续 `1..N` 的映像才可写入新 RAW 文件。

| 验收层 | 自动化证据 | 必须保持的边界 |
|---|---|---|
| 核心解析与导出 | 合成 TD0 覆盖头/注释、A097 CRC、普通原始/重复模式/RLE 数据、严格 CHS 证明、RAW 字节顺序、源 SHA-256 不变和已有输出拒绝。 | 绝不写 TD0 源文件，不覆盖、不输出设备；只创建新本地 RAW 文件。 |
| 结构拒绝 | 覆盖小写 `td` 高级压缩、损坏 CRC、扇区标志、尾随字节、重复或不完整坐标、逻辑/物理不一致、非连续 ID 与无证明布局。 | 不解压高级压缩，不猜测、补零、重排或修复扇区；不能将异常介质扁平化。 |
| CLI、SDK、桌面与清单 | CLI JSON `td0-info`/`convert-td0`、SDK `inspect_td0()`/`export_td0_to_raw()`、清单 TD0 格式发现、离屏桌面独立动作、文件选择器直达只读检查器和七语种文本均有严格回归。 | 不接入 batch v4、文件系统浏览/编辑、设备、TD0 写入、修复、磁通或位流处理。 |

## v0.10 只读批量映像清单与条件报告验收

该流程只扫描一个已有且不是符号链接的本地目录，并只读取已知映像后缀的常规文件元数据。筛选可组合递归、扩展名、已识别映像格式、文件系统、字节范围和 SHA-256 前缀；SHA-256 与分区摘要均为显式可选项。报告写入新的 JSON、CSV 或 HTML 文件，源候选不会被打开为可写文件系统，也不会被修改。

| 验收层 | 自动化证据 | 必须保持的边界 |
|---|---|---|
| 核心筛选与观察 | 合成目录覆盖非递归/递归发现、已知后缀、格式/文件系统/大小/SHA-256 前缀筛选，以及可选 SHA-256 与分区摘要；报告保存相对路径、元数据和可审计错误。 | 不跟随符号链接，不扫描物理设备；最多发现 10,000 个常规文件，单文件超过 16 GiB 时不进入报告。 |
| 报告输出 | JSON、CSV、HTML 均由同一清单产生；源文件 SHA-256 在扫描后保持不变；取消和失败不遗留目标。 | 只接受新目标，使用临时同级文件和无覆盖提升；已有目标、符号链接目标、扫描根目录内目标和不存在的目标父目录均被拒绝。 |
| CLI、SDK 与桌面 | `inventory-images` 的 JSON 摘要、`DiskForgeClient.inventory_images()`/`export_image_inventory()`、离屏 GUI 始终可用动作与七语种界面均有严格回归。 | 不接入批处理 v4；不执行或挂载映像内内容，不进行映像写入、覆盖、设备路径或取证完整性声明。 |

## v0.10 CPC 标准/扩展 DSK 只读检查与严格 RAW 导出验收

该能力只接受双签名确认的标准或扩展 CPC DSK 扇区容器。它可以报告容器、磁道与扇区结构；仅在精确 EOF、完整物理 cylinder/side、清洁控制器状态、固定几何、连续扇区 ID 和恰好一份正常长度数据都被证明时，才输出新的 RAW 文件。它不是通用 `.dsk` 解释器、文件系统或编辑器。

| 验收层 | 自动化证据 | 必须保持的边界 |
|---|---|---|
| 核心 | 合成标准/扩展头、单/双面物理顺序、轨道与扇区描述符、严格 RAW 字节、源 SHA-256 不变、原子新目标和取消。 | 仅签名识别；不写源、不覆盖、不写设备。 |
| 严格拒绝 | 签名、截断/声明长度、未格式化轨道、状态位、C/H 不一致、非连续/重复 ID、短/长/多份扇区数据与同路径输出均有回归。 | 不对弱扇区、复制保护、位流、磁通、启动性或文件系统语义作声明。 |
| 入口 | CLI JSON `cpc-dsk-info`/`convert-cpc-dsk`、SDK、格式识别/通用转换拒绝、只读清单发现、桌面工具动作和 `.dsk` 直达检查器、七语种文本均经回归。 | 无文件系统会话、通用转换、编辑、修复或批处理写入入口。 |

## v0.10 D88 只读检查与严格 RAW 导出验收

该能力仅接受一个精确大小、首个轨道偏移为 0x2A0/0x2B0 的 D88 磁盘。它能够报告轨道和扇区结构；仅在偏移表、扇区边界、完整物理几何、状态、删除标志和数据长度均能严格证明时，才输出新的 RAW。它不是通用 D88 编辑器、文件系统或多盘容器读取器。

| 验收层 | 自动化证据 | 必须保持的边界 |
|---|---|---|
| 核心 | 合成 0x2A0 单盘、偏移表、扇区头、严格 RAW 字节、源保存、既有目标拒绝及尾随/多盘拒绝。 | 仅首盘精确 EOF；不写源、不覆盖、不写设备。 |
| 严格拒绝 | 状态或删除标记、无效范围、异常扇区数据和不完整矩形几何均拒绝 RAW 导出。 | 不对弱/错误扇区、复制保护、位流、磁通、启动性或文件系统语义作声明。 |
| 入口 | CLI JSON `d88-info`/`convert-d88`、SDK、格式识别/通用转换拒绝、清单、桌面动作与 `.d88`/`.1dd`/`.2dd` 直达路由、七语种文本均已覆盖。 | 无文件系统会话、通用转换、编辑、修复或批处理写入入口。 |

## v0.10 显式 FAT 元数据批量编辑验收

该能力只修改已打开可写 FAT 映像或显式选择 FAT 分区中的**明确条目路径**。请求必须至少包含一个标准 DOS 属性位变更或一个无时区 FAT 创建、修改、访问时间字段；预览阶段只验证配方形状并标记写入。核心按调用方路径顺序逐项应用并返回已完成项，因此不会虚构跨多个 FAT 目录项的全有全无事务。

| 验收层 | 自动化证据 | 必须保持的边界 |
|---|---|---|
| 核心 | 合成 FAT12/FAT16/FAT32 覆盖多个显式路径、DOS 属性设/清、部分时间字段、年份与时区限制、只读拒绝及取消前不修改后续路径。 | 仅标准 read-only/hidden/system/archive 位和 FAT 时间；不改内容、ACL、ADS、所有权或名称，不进行递归、通配或自动筛选。 |
| CLI 与 SDK | CLI JSON `set-fat-metadata` 和 SDK `set_fat_metadata()` 覆盖多路径属性/时间组合、逐项审计结果和空请求拒绝。 | 仅可写 FAT；根/重复路径、无字段、时区时间、不可表示年份、非 FAT 与设备路径均拒绝。 |
| 批处理、设计器与桌面 | schema v4 `fat_metadata` 预览标记 `will_write: true`，执行后复查映像元数据；图形设计器序列化/回填路径、属性设定/清除和时间；桌面支持多选属性 tri-state 与可选创建/修改/访问时间字段，七语种文本均有回归。 | 批处理继续拒绝设备；预览不读取或写入映像；多项目录更新不承诺回滚事务。 |

## v0.10 FAT 已删除根目录单簇候选恢复验收

该能力是只读源映像的保守候选复制，不是通用数据恢复。核心使用 BPB 推导 FAT12/FAT16 固定根目录和数据区，只扫描 `0xE5` 标记的普通 8.3 文件槽位；候选必须大小大于 0、不超过一个簇、起始簇有效且其当前 FAT 项为空闲。恢复前重新读取槽位和 FAT 项，仅将声明的字节数写入一个此前不存在的本地输出文件。删除槽位中的首字符已经丢失，而空闲簇仍可能包含陈旧或重写数据，因此不对原文件名或完整性作任何断言。

| 验收层 | 自动化证据 | 必须保持的边界 |
|---|---|---|
| FAT 核心 | 合成 FAT12 与 FAT16 映像构造标准删除槽位和空闲首簇；候选扫描后恢复的输出与原始短文件字节相同，源 SHA-256 保持不变。 | 只扫描固定根目录普通 8.3 槽位；FAT32、子目录、长名称、零长度、多簇链、非空闲簇、无效 BPB 与越界范围均拒绝或标记不可恢复。 |
| 输出与取消 | 已存在输出被拒绝；恢复只在目标目录创建临时文件并采用无覆盖提升；取消测试验证抛出取消异常且无目标残留。 | 不覆盖本地文件，不在源映像、分区或设备内恢复，不生成完整性或原名保证。 |
| CLI、SDK 与桌面 | CLI JSON `list-deleted-fat`/`recover-deleted-fat`、SDK `list_deleted_fat()`/`recover_deleted_fat()`、七语种桌面动作和显式 FAT 分区只读工作器均有回归。 | 批处理 v4 不接入无人值守恢复；容器临时会话、非 FAT 和物理设备均不能作为恢复源。 |

## v0.10 FAT 文件与目录树管理验收

FAT 跨目录移动接受一个映像内常规文件或完整目录树和一个**已经存在**的映像内目录。核心先标准化路径并拒绝根目录、缺失源、缺失目标、非目录目标、同名目标、只读会话和位于源树内的目录目标；常规文件使用底层移动，目录树先可取消复制到全新目标再删除源树。复制完成前取消会保留源树；源树删除失败也保留两棵完整树以便人工处理，因此目录移动不宣称为原子事务。成功后源路径消失、目标路径存在，提取后的载荷必须逐字节一致。相同父目录的改名要求一个非空、无分隔符的新名称，拒绝同名目标，且不会覆盖现有条目。

| 验收层 | 自动化证据 | 必须保持的边界 |
|---|---|---|
| FAT 核心 | 合成 FAT12/FAT16 映像验证移动后可提取的原始载荷、同名冲突、无效目标、根目录、目录源与只读拒绝；并验证新空目录的持久化重开、嵌套父级、重复、缺失/文件父级、根目录、只读与取消前拒绝，以及常规文件和完整目录树复制后的源/目标载荷、冲突、源树内目标、无效目标、只读和取消清理。核心重命名验证成功路径、冲突、含分隔符的新名称与只读拒绝；删除覆盖文件和目录树、根/空/重复请求与只读拒绝。 | 文件与目录树移动均不覆盖、不合并、不创建目标目录；目录移动为可取消复制后删除，源树内目标被拒绝且不宣称原子性。空目录创建只接受一个新路径，且父目录必须已经存在；不覆盖、不会隐式递归建树。文件或目录树复制保留源，要求既有目标目录和新同名目标；目录目标不得位于源树中，失败时尽力清除新目标树。重命名仅限同一父目录下的一个条目，拒绝空名称、分隔符与覆盖。删除仅接受一个经预检的显式非根条目；目录树删除不可逆且不宣称事务性。 |
| CLI 与 SDK | `move-fat IMAGE SOURCE_PATH TARGET_DIRECTORY` 输出 JSON `source`/`destination`；`copy-fat IMAGE SOURCE_PATH TARGET_DIRECTORY` 可复制常规文件或目录树且输出同一 JSON；`mkdir-fat IMAGE DIRECTORY_PATH` 输出 JSON `directory`；`delete-fat IMAGE ITEM_PATH` 输出 JSON `path`；通用 CLI `rename` 与 `DiskForgeClient.rename_fat()` 均验证无覆盖的同目录改名。`DiskForgeClient.move_fat()`、`copy_fat()`、`create_fat_directory()` 与 `delete_fat()` 覆盖一等 FAT 入口；删除返回已请求路径。 | 仅可写 FAT；可选显式 FAT 分区由既有验证路由处理；非 FAT 不升级为可写。 |
| 批处理与桌面 | schema v4 `move`、`fat_mkdir`、`fat_copy`、`fat_rename` 与 `fat_delete` 均预览标记 `will_write: true`，执行后审计原映像；图形设计器序列化并回填源映像、条目/目录路径、新名称和分区索引；桌面目录创建动作仅在可写 FAT 会话启用，复制动作对单个常规文件或目录启用；`fat_delete` 图形配方保存/重开显式条目路径与分区并显示不可逆提示。 | 无人值守配方不接受设备；桌面可对单选目录显示受控移动和复制动作；目录创建不接受分隔符、根路径或隐式父级；复制不接受覆盖、源树内目标或多选；重命名不允许覆盖；全部七种界面语言均有无回退目录。 |

v0.10.0 正式发行的完整严格命令 `QT_QPA_PLATFORM=offscreen pytest -W error` 已验证为 **726 passed, 15 skipped**，且没有警告。跳过项对应可选的外部集成或真实文件系统夹具，不影响上述合成 FAT 元数据/移动、显式选择的 ZIP 映像容器、FAT 删除候选恢复、IMD、TD0、批量映像清单、PCE PFI v0、WOZ2、A2R3、D64、D71、D81、G64 v0、G71 v0 与 P64 v0 跨入口检查和以下历史容器回归。

## v0.10 历史容器只读检查与受限 RAW 导出验收

本组能力将每一种容器独立解析，且不因扩展名将它们降级为可写 RAW 或 FAT 映像。传统 RAW 别名是受限例外：`.vfd`、`.flp` 与容量后缀必须同时落在内置常规 512 字节 PC 软盘精确字节大小白名单内，才在已完成签名容器检查后标识为 RAW；构造回归覆盖 FAT12 `.vfd` 浏览、同字节未知后缀与错误尺寸拒绝、清单候选以及安全 ZIP 载荷物化后保留后缀的重识别。多映像 ZIP 构造回归进一步验证默认歧义拒绝、1–64 个根级载荷的全量安全校验、缺失载荷拒绝、显式载荷物化、CLI `zip-info` 与 `list --zip-payload` JSON、SDK 载荷清单/会话、桌面只读会话、临时清理和源 SHA-256 不变。所有合成测试同时覆盖格式识别、通用转换拒绝、只读清单发现、CLI JSON、SDK、文件系统会话拒绝、桌面直达专用检查器和七语种用户可见文本。可导出的项目只在源不变、目标此前不存在且结构能证明安全平面化时创建新的本地 RAW；位流项目不提供扇区解码或 RAW 输出。

| 格式 | 核心自动化证据 | 明确边界 |
|---|---|---|
| HFE | 验证 v1/v2/v3 签名、512 字节头、LUT、块对齐范围和双侧交错统计。 | 只做结构检查；不解码位流、不导出 RAW、不浏览或写入。 |
| DC42 | 验证 84 字节头、精确数据/标签 fork、数据校验与历史标签跳过规则；验证数据 fork 原样新 RAW 输出。 | 不修改源或标签 fork；无容器写入、文件系统或通用转换。 |
| 2MG/2IMG | 验证 64 字节头、格式/flags、连续注释/创建者块及 DOS/ProDOS 数据块输出。 | NIB 与不规则布局仅可检查；不写回容器。 |
| APRIDISK | 覆盖固定头、普通/RLE 扇区、注释/创建者、删除记录以及严格矩形排序输出。 | 删除/不规则扇区不导出；不把 `.dsk` 后缀当作格式保证。 |
| CopyQM | 覆盖可校验头、注释、符号长度 RLE、掩码 CRC 和固定 DOS 几何 RAW。 | 仅完整校验布局可导出；不支持不明变体或写回。 |
| SAP | 覆盖 Pukall 头/CRC、数据去混淆、保护状态和常规 256B 矩形扇区输出。 | 有保护、CRC 或布局异常时只拒绝，不猜测或修复。 |
| MSA | 覆盖大端头、未压缩/E5-RLE 轨道、真实 E5 数据、双面顺序和精确 EOF。 | 不完整轨道、RLE 异常和尾随数据均不导出。 |
| PSI | 覆盖每个 chunk CRC、SECT/DATA、压缩填充、元数据、严格矩形 geometry 和新 RAW。 | weak/alternate/坏 CRC/不规则或未知路径不扁平化。 |
| PRI | 覆盖每个 chunk CRC、轨道位数/时钟、DATA、FUZZ/BCLK/WEAK 事件范围与无数据轨报告。 | 仅结构检查；不解码位流、不导出 RAW、不浏览或写入。 |
| 86F | 覆盖 v2.12 固定 RPM/总 bitcell 子集、单/双面偏移表、surface 描述及精确轨范围。 | zoned、压缩、非总 bitcell/变速变体和所有解码/RAW/写入路径均拒绝。[10] |
| FDI | 覆盖 v2.0 签名和头、注释、轨道目录类别、声明字节范围与精确偏移核算；CLI JSON、SDK、清单、桌面直达和七语种文本均有回归。 | 只做多层容器结构检查；不解码轨道、不导出 RAW、不浏览、不转换或写入。 |
| JV3 | 覆盖头块、占用槽、写保护、正常扇区、严格矩形几何、严格 RAW 字节、无覆盖输出和跨入口拒绝。 | 仅完整、连续、固定尺寸且正常的扇区布局可导出；异常标志、非正常 DAM、CRC/几何或槽位异常不扁平化。 |
| DMK | 覆盖 16 字节原生头、单/双面交错固定轨道、保留/真实驱动器标记、IDAM 指针有序性、范围、密度位与 `0xFE` 标记；CLI JSON、SDK、清单、桌面直达和七语种文本均有回归。 | 只检查原生头和 IDAM 目录；不解释 FM/MFM、数据标记或 CRC，不提取扇区、不导出 RAW、不浏览、不转换、不修复或写入。 |
| UDI v1.0 | 覆盖大写 `UDI!` 头、声明长度、单/双面交替 MFM 轨道、扩展头、轨道数据/时钟图范围、时钟图未用尾位和公开 CRC32；同时验证畸形、截断、尾随、转换/会话拒绝、CLI JSON、SDK、清单、桌面直达和七语种文本。 | 仅接受原生大写 v1.0 MFM 轨道容器；不接受压缩小写 UDI 或后续/未知轨道模式，不解码 MFM 或扇区、不导出 RAW、不浏览、不转换、不修复或写入。 |
| SCP | 覆盖标准软盘头、168 项轨道表、头选择奇偶性、匹配 `TRK` 记录、每轨 revolution 的时长/通量字/偏移范围、通量范围非重叠和 header 校验和；同时验证非法模式、截断、转换/会话拒绝、CLI JSON、SDK、清单、桌面直达和七语种文本。 | 仅接受标准软盘、只读、无 footer、非扩展模式且 16-bit 通量字的 SCP 子集；不解码通量、不导出 RAW、不浏览、不转换、不修复或写入。 |
| HxC MFM | 覆盖打包 `HXCMFM` 头、轨道/面/速率字段、规范 cylinder/side 目录、512 字节零填充、轨道范围不重叠和精确 EOF；同时验证非零填充、重复坐标、尾随字节、通用转换/会话拒绝、CLI JSON、SDK、清单、桌面直达和七语种文本。 | 仅接受规范有序、连续对齐的 HxC MFM 子集；轨道字节保持不透明，不解码 MFM/FM 或扇区、不导出 RAW、不浏览、不转换、不修复或写入。 |
| PCE PFI v0 | 覆盖 `PFI ` 首块/版本、大端 chunk 语法、初值零的公开 CRC-32、唯一轨道上下文、非零时钟、单调且四字节对齐的索引、每轨唯一 DATA、受限脉冲令牌、零长度 `END ` 与精确 EOF；同时验证 CRC/截断/重复坐标/无效脉冲/尾随拒绝、通用转换与会话拒绝、CLI JSON、SDK、清单、桌面直达和七语种文本。 | 仅接受规范 PFI v0 子集；通量字节保持不透明，不解码通量、MFM/FM 或扇区，不导出 RAW、不浏览、不转换、不修复或写入。 |
| WOZ 2.0/2.1 | 合成 WOZ2 覆盖签名、可选 CRC、INFO v2/v3、规范 INFO/TMAP/TRKS 顺序、映射位流范围、FLUX 一致性、UTF-8 META、精确 EOF 和结构拒绝；同时验证格式识别、通用转换/会话拒绝、CLI JSON `woz-info`、SDK、清单、桌面 `.woz` 直达和七语种文本。官方公开样本的 WOZ 2.0 与含 FLUX 的 WOZ 2.1 文件也在私有研究目录中经只读 CLI 成功检查，未被提交或执行。 | 仅接受规范 WOZ2 容器；不接受 WOZ1，不解码位流、通量或扇区，不导出 RAW、不浏览、不转换、不修复或写入。 |
| A2R 3.x | 合成 A2R3 覆盖固定签名、唯一首个 INFO v1、受限 little-endian chunk 边界、RWCP 采集、SLVD 已求解轨道、UTF-8 META、精确 EOF 与结构拒绝；同时验证格式识别、通用转换/会话拒绝、CLI JSON `a2r-info`、SDK、清单、桌面 `.a2r` 直达和七语种文本。 | 仅接受规范 A2R3 容器；不接受 A2R1/A2R2，不解码通量、位流或扇区，不导出 RAW、不浏览、不转换、不修复或写入。 |
| D64（规范 35 轨） | 合成 D64 覆盖精确 174,848 字节、256 字节扇区、BAM 版本/可用块计数、目录链、PETSCII 安全名称、普通 SEQ/PRG/USR 数据链、零块条目、最终扇区字节数、目录循环、REL、块数不符和 BAM 异常拒绝；同时验证格式识别、只读清单、CLI JSON `d64-info`、SDK 结构检查/会话/提取、批处理只读会话、安全 ZIP 载荷物化、通用转换与可写会话拒绝、源 SHA-256 不变和桌面 `.d64` 直达。[12] | 仅接受无错误表的规范 35 轨 D64；可列出/提取已验证的普通 SEQ/PRG/USR 文件，但不支持 40 轨/错误表变体、REL/GEOS、GCR 解码、修复、通用转换、新建、编辑、写入或设备路径。 |
| D71（规范 70 轨双面） | 合成 D71 覆盖精确 349,696 字节、256 字节扇区、双面标志、侧 0 四字节 BAM 条目、侧 1 分离位图/空闲块计数、目录链、跨侧普通 SEQ/PRG/USR 数据链、最终扇区字节数、目录循环、系统扇区重叠、REL、位图/计数异常与错误大小拒绝；同时验证格式识别、只读清单、CLI JSON `d71-info`、SDK 结构检查/会话/提取、批处理只读会话、安全 ZIP 载荷物化、通用转换与可写会话拒绝、源 SHA-256 不变和桌面 `.d71` 直达。[12] | 仅接受无错误表的规范 70 轨 D71；可列出/提取已验证的普通 SEQ/PRG/USR 文件，但不支持 40 轨/错误表变体、REL/GEOS、GCR 解码、修复、通用转换、新建、编辑、写入或设备路径。 |
| D81（规范 80 轨双面） | 合成 D81 覆盖精确 819,200 字节、256 字节扇区、1581 头、匹配磁盘 ID、两个 40 条目 BAM、40 位位图/空闲块计数、规范 40 轨目录、侧 1 普通 SEQ/PRG/USR 数据链、最终扇区字节数、系统扇区保留、REL/扩展目录、位图/计数异常与错误大小拒绝；同时验证格式识别、只读清单、CLI JSON `d81-info`、SDK 结构检查/会话/提取、批处理只读会话、安全 ZIP 载荷物化、通用转换与可写会话拒绝、源 SHA-256 不变和桌面 `.d81` 直达。[12] | 仅接受无错误表、规范目录的 80 轨 D81；可列出/提取已验证的普通 SEQ/PRG/USR 文件，但不支持扩展目录、REL/GEOS/CBM 分区、GCR 解码、修复、通用转换、新建、编辑、写入或设备路径。 |
| G64 v0 | 合成 G64 v0 覆盖固定 `GCR-1541` 版本 0 签名、受限小端轨道/速度表、不透明存储轨道长度和范围、恒定与映射速度区、非重叠分配、精确 EOF 与结构拒绝；同时验证格式识别、通用转换/会话拒绝、CLI JSON `g64-info`、SDK、清单、桌面 `.g64` 直达和七语种文本。 | 仅接受规范 1541 G64 v0 容器；不接受 `GCR-1571`，不解码 GCR 或扇区，不导出 RAW、不浏览、不转换、不修复或写入。 |
| G71 v0 | 合成 G71 v0 覆盖固定 `GCR-1571` 版本 0 签名、恰好 168 个半轨条目、受限小端轨道/速度表、不透明存储轨道长度和范围、恒定与映射速度区、非重叠分配、精确 EOF 与结构拒绝；同时验证格式识别、通用转换/会话拒绝、CLI JSON `g71-info`、SDK、清单、桌面 `.g71` 直达和七语种文本。[12] | 仅接受规范双面 1571 G71 v0 容器；GCR 字节保持不透明，不解码 GCR 或扇区，不导出 RAW、不浏览、不建立文件系统会话、不转换、不修复或写入。 |
| P64 v0 | 合成 P64 v0 覆盖固定 `P64-1541` 版本 0 签名、已定义标志、全流与逐块 CRC-32、受限 HTPx 框架、唯一半轨/磁头面坐标、范围流字节数、空 `DONE` 终止、精确 EOF 与结构拒绝；同时验证格式识别、通用转换/会话拒绝、CLI JSON `p64-info`、SDK、清单、桌面 `.p64` 直达和七语种文本。 | 仅接受规范 1541 P64 v0 容器；范围编码 NRZI 数据保持不透明，不解码脉冲、GCR 或扇区，不导出 RAW、不浏览、不转换、不修复或写入。 |

## UFI USB 软驱人工验收

Linux 上的受控 UFI USB 软驱格式化要求 `ufiformat`、相应权限、真正的 UFI 兼容设备以及可丢弃介质。DiskForge 仅在 sysfs 将 `/dev/sgN` 显式关联到已发现的可移动块设备时显示候选项；随后必须由 `ufiformat -i` 报告设备与可用容量。操作者必须选择报告中的一个容量并输入 `FORMAT_FLOPPY`；执行命令固定包含 `-V` 验证，且不会使用跳过安全检查的 `-F` 选项。[5]

UFI 格式化完成后，创建 FAT 文件系统是**独立操作**，需要再次选择设备和确认。没有真实硬件读写回验前，任何环境都不应声称已验证特定 USB 软驱型号或控制器。该实硬件步骤记录为后续设备兼容性证据，而不是 v1.0.0 功能对齐的阻塞条件。

## 本机已复现实例

下表记录开发环境中一次可复现的只读验收，仅作为环境证据，不替代用户在目标系统、目标设备或目标样本上的验证。

| 路径 | 后端 | 结果 |
|---|---|---|
| CC0 NTFS `ntfs-lastaccess.raw` | Sleuth Kit 4.12.1 | 在 LBA 128 处列举 `/test`，提取 `/test/1.txt`，143 字节。 |
| MIT EXT4 `fs.ext4` | Sleuth Kit 4.12.1 | 在 LBA 2048 处列举 `/audio1`，提取 `/audio1/debian.mp3`，69,727 字节。 |
| NPS journaled HFS+ `image.gen1.dmg` | Sleuth Kit 4.12.1 | 列举 `/file1.txt` 与 `/file2.txt`，提取 `/file1.txt`，28 字节。 |
| 合成 standalone NTFS | ntfs-3g/ntfsprogs 2022.10.3 | 预演并向新副本添加 `PAYLOAD.TXT`；读回哈希匹配且源 SHA-256 不变。 |
| 合成 standalone EXT4 | e2fsprogs 1.47.0 | 通过 `debugfs` undo 日志写入新副本；读回哈希匹配，`e2fsck -fn` 返回 0，源 SHA-256 不变。 |
| 合成 standalone 经典 HFS 注入 | hfsutils 3.2.6-16 | 创建 800 KiB 卷，在隔离 `HOME` 中经 `hmount`、`hcopy -r`、`hls` 运行；新副本读回哈希匹配，源 SHA-256 不变。`hls` 对不存在目标返回 0 但在 stderr 写出缺失诊断，因此该诊断而非退出码构成无覆盖预检。 |
| 合成 standalone 经典 HFS 新建 | hfsutils 3.2.6-16 | 将新的 819,200 字节常规文件格式化为 `DISKFORGE` 卷；`inspect_image()` 识别为 HFS，输出 SHA-256 与审计结果一致；818,176 字节文件被 hformat 明确拒绝，隔离状态仅产生私有 `.hcwd`。 |
| 官方 WOZ 2.0 与 WOZ 2.1（含 FLUX）样本 | DiskForge `woz-info` | 官方公开示例归档的一个 WOZ 2.0 与一个含 FLUX 的 WOZ 2.1 文件均成功完成只读结构检查；归档仅在私有研究目录中使用，未执行、修改或提交。 [11] |

## References

[1]: https://github.com/msuhanov/ntfs-samples "msuhanov/ntfs-samples"
[2]: https://github.com/eribertomota/forensics-samples "eribertomota/forensics-samples"
[3]: https://digitalcorpora.org/corpora/disk-images/ "Digital Corpora Disk Images"
[4]: https://downloads.digitalcorpora.org/corpora/drives/nps-2009-hfsjtest1/ "NPS HFS Journal Test Image Directory"
[5]: https://manpages.ubuntu.com/manpages/jammy/man8/ufiformat.8.html "ufiformat manual"
[10]: https://86box.readthedocs.io/en/v6.0/dev/formats/86f.html "86F Specification for v2.12 Extended"
[6]: https://linux.die.net/man/8/ntfscp "ntfscp(8) — copy file to an NTFS volume"
[7]: https://manpages.debian.org/testing/e2fsprogs/debugfs.8.en.html "debugfs(8) — ext2/ext3/ext4 filesystem debugger"
[8]: https://manpages.ubuntu.com/manpages/bionic/man1/hfsutils.1.html "hfsutils(1) — classic HFS utility suite"
[9]: https://manpages.ubuntu.com/manpages/jammy/man1/hls.1.html "hls(1) — list files in an HFS directory"
[10]: https://manpages.ubuntu.com/manpages/jammy/man1/hformat.1.html "hformat(1) — create a new HFS filesystem"
[11]: https://applesaucefdc.com/woz/reference2/ "WOZ 2.1 Reference"
[12]: https://vice-emu.sourceforge.io/vice_17.html "VICE Disk Image Formats"
